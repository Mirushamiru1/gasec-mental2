
GASEC Mental Health — Full Dynamic Rebuild (B)
This package is a full dynamic-ready React + Firebase starter, PWA-enabled, with sound and images.
Follow FINAL_STEPS.txt to deploy to Netlify (frontend) and Firebase (backend).
